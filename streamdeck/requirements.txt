# AutoPi Stream Deck controller dependencies.
#   pip install -r streamdeck/requirements.txt

# Pure-Python driver for Elgato Stream Deck hardware. 0.9.8 is the floor: it
# adds the USB product id used by current XL / Module 32 revisions.
streamdeck>=0.9.8

# Key image rendering.
Pillow>=10.4.0

# HTTP client for the AutoPi API.
httpx>=0.27.0
