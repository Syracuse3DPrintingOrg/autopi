# Core web app. These must install for the app to run; they are all pure-Python
# with wheels on every platform AutoPi targets (including the Raspberry Pi).
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
itsdangerous>=2.1
python-multipart>=0.0.9
pydantic-settings>=2.2
httpx>=0.27

# Local database (actions, vehicle profiles, CAN messages, logic rules) stored
# as a SQLite file under data_dir. See service/app/db/.
sqlalchemy>=2.0
