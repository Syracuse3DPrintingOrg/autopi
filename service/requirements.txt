# Core web app.
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
itsdangerous>=2.1
python-multipart>=0.0.9
pydantic-settings>=2.2
httpx>=0.27

# GPIO on a Raspberry Pi. Harmless to install elsewhere: the driver reports
# itself unavailable when there is no real pin factory, so the app still runs.
gpiozero>=2.0

# Phase 2 (CAN) will add:
# python-can>=4.3
